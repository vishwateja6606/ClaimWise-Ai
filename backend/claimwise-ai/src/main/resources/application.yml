server:
  port: 8081

spring:
  application:
    name: claimwise-ai

  datasource:
    url: jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres?sslmode=require
    username: postgres.zxprwlkozitevixndprx
    password: Vishwa@6606

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
app:
  jwt:
    secret: mysupersecuresecretkey1234567890abcd
    expiration-ms: 86400000
management:
  endpoints:
    web:
      exposure:
        include: health,info